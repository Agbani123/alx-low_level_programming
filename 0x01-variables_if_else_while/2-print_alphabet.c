#include <stdio.h>
/**
 * main - Print alphabets in lowercase
 *Return:always 0
 */
int main(void)
{
char ch;
putchar("Alphabets from a-z are: \n");
for (ch = 'a'; ch <= 'z'; ch = ++)
{
putchar("%c\n", ch);
}
return (0);
}
